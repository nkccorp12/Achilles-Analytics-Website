---
phase: 04-global-polish-footer
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/variants/VariantNeon.css
  - src/variants/VariantGlass.css
autonomous: true

must_haves:
  truths:
    - "All CSS files use only 640px and 1024px breakpoints (no 600px, 768px, 900px, 1100px)"
    - "Legacy variant responsive rules still apply correctly at the normalized breakpoints"
    - "Footer items and CTAs stack vertically on mobile (already complete — verification only)"
  artifacts:
    - path: "src/variants/VariantNeon.css"
      provides: "Normalized breakpoints: 1100px->1024px, 768px->640px"
      contains: "max-width: 1024px"
    - path: "src/variants/VariantGlass.css"
      provides: "Normalized breakpoints: 900px->1024px, 600px->640px"
      contains: "max-width: 1024px"
  key_links:
    - from: "src/variants/VariantNeon.css"
      to: "Project breakpoint standard"
      via: "Media query values match 640px/1024px"
      pattern: "max-width: (640|1024)px"
    - from: "src/variants/VariantGlass.css"
      to: "Project breakpoint standard"
      via: "Media query values match 640px/1024px"
      pattern: "max-width: (640|1024)px"
---

<objective>
Normalize all non-standard CSS breakpoints in legacy variant files to the project standard (640px / 1024px), and verify footer responsiveness is already complete.

Purpose: Satisfies GLOBAL-01 (standardized breakpoints across all CSS), GLOBAL-02 (consistent viewport behavior), and REACH-01 (footer mobile stacking — verification only, already implemented).
Output: Updated VariantNeon.css and VariantGlass.css with normalized breakpoints.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/variants/VariantNeon.css
@src/variants/VariantGlass.css
@src/pages/NeuPage.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize VariantNeon.css and VariantGlass.css breakpoints</name>
  <files>src/variants/VariantNeon.css, src/variants/VariantGlass.css</files>
  <action>
    **VariantNeon.css** — Two breakpoints need normalization:

    1. Line 1264: Change `@media (max-width: 1100px)` to `@media (max-width: 1024px)`
       - This is the tablet breakpoint. Rules inside remain unchanged (padding, grid collapses, sphere sizing).
       - The 76px difference (1100->1024) is negligible for these layout rules.

    2. Line 1358: Change `@media (max-width: 768px)` to `@media (max-width: 640px)`
       - This is the mobile breakpoint. Rules inside remain unchanged (padding, font sizes, CTA stacking, council layout).
       - The 128px shift means these rules activate slightly later on wider phones. This is acceptable — the tablet rules at 1024px cover the 640-768px gap.

    **VariantGlass.css** — Two breakpoints need normalization:

    1. Line 834: Change `@media (max-width: 900px)` to `@media (max-width: 1024px)`
       - This is the tablet breakpoint. Rules inside remain unchanged (grid collapses, card layouts, council flex).
       - The 124px widening means tablet rules apply slightly earlier. This is desirable — catches more tablet devices.

    2. Line 882: Change `@media (max-width: 600px)` to `@media (max-width: 640px)`
       - This is the mobile breakpoint. Rules inside remain unchanged (hero padding, CTA stacking, node sizing).
       - The 40px widening is minor and catches more small phones.

    **Important context:** These variant files (VariantNeon, VariantGlass) are NOT currently loaded by App.jsx — it only imports NeuPage. However, normalizing ensures consistency if variants are ever re-enabled. The VariantGrid.css already uses 640px/1024px (lines 507, 531) and needs no changes.

    Do NOT modify any CSS rules inside the media query blocks — only change the breakpoint values in the @media declarations.
    Do NOT modify VariantGrid.css — it already uses correct breakpoints.
    Do NOT modify any files in src/pages/ — they already use correct breakpoints.
  </action>
  <verify>
    Run a grep across all CSS files to confirm no non-standard breakpoints remain:

    ```bash
    grep -rn "@media.*max-width" src/ --include="*.css" | grep -v "640px\|1024px\|prefers-reduced-motion"
    ```

    Expected: Zero results (all breakpoints normalized to 640px or 1024px).

    Also verify the files still parse correctly by checking no syntax errors:
    ```bash
    # Quick syntax sanity: count opening vs closing braces
    grep -c '{' src/variants/VariantNeon.css
    grep -c '}' src/variants/VariantNeon.css
    grep -c '{' src/variants/VariantGlass.css
    grep -c '}' src/variants/VariantGlass.css
    ```
  </verify>
  <done>
    All CSS files across the project use only 640px and 1024px breakpoints. VariantNeon.css normalized from 1100px/768px. VariantGlass.css normalized from 900px/600px. No non-standard breakpoints remain in any CSS file.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify footer responsiveness (REACH-01 confirmation)</name>
  <files>src/pages/NeuPage.css</files>
  <action>
    This is a verification-only task. The footer (neu-reach section) already has complete mobile rules in NeuPage.css at the 640px breakpoint (lines 930-948):

    - `.neu-reach` padding reduces to `64px 16px 48px`
    - `.neu-reach__ctas` stacks with `flex-direction: column`
    - `.neu-reach__cta` centers with `text-align: center; justify-content: center`
    - `.neu-reach__footer` stacks with `flex-direction: column; align-items: center; gap: 12px`

    Verify these rules exist and are correct. Do NOT modify NeuPage.css unless a defect is found.

    Check that:
    1. The 640px media query block contains the neu-reach rules
    2. CTAs stack vertically (flex-direction: column)
    3. Footer items stack and center
    4. No horizontal overflow risk (max-width or padding constraints in place)

    If all rules are present and correct, mark REACH-01 as satisfied with no changes needed.
  </action>
  <verify>
    Read NeuPage.css and confirm the 640px media query contains all four neu-reach mobile rules. No code changes should be needed — this task validates existing work.
  </verify>
  <done>
    Footer responsiveness confirmed: CTAs stack vertically, footer items center on mobile, padding reduces appropriately. REACH-01 satisfied by existing implementation from Phase 1.
  </done>
</task>

</tasks>

<verification>
- Run breakpoint audit grep: `grep -rn "@media.*max-width" src/ --include="*.css"` — all results show only 640px or 1024px
- Verify NeuPage.css footer rules exist at 640px breakpoint
- No files in src/pages/ were modified (already correct)
- Only VariantNeon.css and VariantGlass.css had breakpoint values changed
</verification>

<success_criteria>
1. Zero non-standard breakpoints remain across all CSS files (GLOBAL-01)
2. All sections use consistent 640px/1024px breakpoint system (GLOBAL-02)
3. Footer CTAs and items stack vertically on mobile — confirmed existing (REACH-01)
4. No regressions — only @media values changed, no CSS rules modified
</success_criteria>

<output>
After completion, create `.planning/phases/04-global-polish-footer/04-02-SUMMARY.md`
</output>
