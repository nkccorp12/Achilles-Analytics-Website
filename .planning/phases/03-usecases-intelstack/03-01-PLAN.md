---
phase: 03-usecases-intelstack
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/UseCases.jsx
  - src/pages/UseCases.css
autonomous: true

must_haves:
  truths:
    - "User can tap showcase card to reveal warehouse background image on touch devices"
    - "User can tap maritime card to reveal maritime background image on touch devices"
    - "User can tap infrastructure card to reveal infra background image on touch devices"
    - "Use case cards display in single-column layout on mobile with proper spacing"
  artifacts:
    - path: "src/pages/UseCases.jsx"
      provides: "useState toggles and onClick handlers for showcase and both cards"
      contains: "useState"
    - path: "src/pages/UseCases.css"
      provides: "Active state CSS classes alongside hover rules"
      contains: "vg-usecase__showcase--active"
  key_links:
    - from: "src/pages/UseCases.jsx"
      to: "src/pages/UseCases.css"
      via: "className with --active modifier"
      pattern: "showcase--active|card--active"
---

<objective>
Add click/tap toggle support to UseCases showcase and cards so background images reveal on touch devices, and verify mobile single-column card layout with proper spacing.

Purpose: CASE-01 and CASE-02 -- hover-only background image reveals are completely inaccessible on touch devices. Users see interactive-looking cards but tapping does nothing. This adds persistent toggle state via React useState so tap reveals work alongside existing hover.
Output: UseCases.jsx with toggle state, UseCases.css with --active classes, verified mobile layout.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/pages/UseCases.jsx
@src/pages/UseCases.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add useState toggles and onClick handlers to UseCases.jsx</name>
  <files>src/pages/UseCases.jsx</files>
  <action>
    Add `import { useState } from 'react';` at top of file.

    Inside `UseCases()` function, add three state variables:
    ```
    const [showcaseActive, setShowcaseActive] = useState(false);
    const [maritimeActive, setMaritimeActive] = useState(false);
    const [infraActive, setInfraActive] = useState(false);
    ```

    On the showcase div (line 124), add:
    - className: change to template literal with conditional `--active` class:
      `className={\`vg-usecase__showcase${showcaseActive ? ' vg-usecase__showcase--active' : ''}\`}`
    - `onClick={() => setShowcaseActive(!showcaseActive)}`

    On the maritime card div (line 168), add:
    - className: `className={\`vg-usecase__card vg-usecase__card--maritime${maritimeActive ? ' vg-usecase__card--active' : ''}\`}`
    - `onClick={() => setMaritimeActive(!maritimeActive)}`

    On the infra card div (line 192), add:
    - className: `className={\`vg-usecase__card vg-usecase__card--infra${infraActive ? ' vg-usecase__card--active' : ''}\`}`
    - `onClick={() => setInfraActive(!infraActive)}`

    Keep all existing props (style, className base) intact. Only add onClick and modify className to include conditional --active.

    Do NOT add role="button", tabIndex, or onKeyPress -- keep changes minimal for this sprint (a11y improvements are out of scope per PROJECT.md).
  </action>
  <verify>Open UseCases.jsx and confirm: (1) useState import exists, (2) three state variables declared, (3) all three interactive elements have onClick handlers and conditional --active classNames.</verify>
  <done>UseCases.jsx has click/tap toggle state for showcase, maritime card, and infra card.</done>
</task>

<task type="auto">
  <name>Task 2: Add --active CSS classes alongside :hover rules in UseCases.css</name>
  <files>src/pages/UseCases.css</files>
  <action>
    Add the following CSS rules. Place them after the existing hover rules for each component, keeping file organization clean.

    **Showcase active states** -- after the existing `.vg-usecase__showcase:hover` rule (line 65-68) and `.vg-usecase__showcase:hover::after` rule (line 88-90):

    Add `cursor: pointer;` and `user-select: none;` to the base `.vg-usecase__showcase` rule (around line 54).

    Modify the existing hover rules to include --active selector using comma separation:
    - Change `.vg-usecase__showcase:hover {` to `.vg-usecase__showcase:hover, .vg-usecase__showcase--active {`
    - Change `.vg-usecase__showcase:hover::after {` to `.vg-usecase__showcase:hover::after, .vg-usecase__showcase--active::after {`

    **Card active states** -- modify existing hover rules:

    Add `cursor: pointer;` and `user-select: none;` to the base `.vg-usecase__card` rule (around line 278).

    - Change `.vg-usecase__card:hover {` (line 297) to `.vg-usecase__card:hover, .vg-usecase__card--active {`
    - Change `.vg-usecase__card--maritime:hover::after, .vg-usecase__card--infra:hover::after {` (line 328-330) to:
      ```css
      .vg-usecase__card--maritime:hover::after,
      .vg-usecase__card--maritime.vg-usecase__card--active::after,
      .vg-usecase__card--infra:hover::after,
      .vg-usecase__card--infra.vg-usecase__card--active::after {
        opacity: 1;
      }
      ```

    **Mobile layout verification** -- the existing 640px media query (line 472-474) already has `grid-template-columns: 1fr` and card padding reduction. Verify these exist and add `gap: 20px;` to the `.vg-usecase__cards` rule inside that media query if not already present.

    Do NOT create separate rule blocks for --active -- modify existing :hover rules with comma-separated selectors for cleaner CSS.
  </action>
  <verify>Search UseCases.css for: (1) `--active` appears in showcase and card selectors, (2) `cursor: pointer` on showcase and card base rules, (3) `grid-template-columns: 1fr` exists in 640px media query.</verify>
  <done>UseCases.css has --active class selectors paired with all :hover rules. Showcase and cards show pointer cursor. Mobile single-column layout confirmed.</done>
</task>

</tasks>

<verification>
1. grep for `useState` in UseCases.jsx -- must find import and 3 state declarations
2. grep for `--active` in UseCases.jsx -- must find 3 conditional classNames
3. grep for `--active` in UseCases.css -- must find matching CSS selectors for showcase and both card types
4. grep for `cursor: pointer` in UseCases.css -- must find on showcase and card base rules
5. Confirm no `@media (max-width: 768px)` exists in UseCases.css (should only use 640px and 1024px)
</verification>

<success_criteria>
- UseCases.jsx imports useState and has 3 toggle states with onClick handlers
- UseCases.css has --active selectors alongside every :hover rule for showcase, maritime card, and infra card
- Cards grid collapses to single column at 640px breakpoint
- No new CSS breakpoints introduced outside the 640/1024 standard
</success_criteria>

<output>
After completion, create `.planning/phases/03-usecases-intelstack/03-01-SUMMARY.md`
</output>
