---
phase: 01-neupage-navigation-hero
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/NeuPage.jsx
  - src/pages/NeuPage.css
autonomous: true

must_haves:
  truths:
    - "User can tap a hamburger icon to open a slide-out nav panel on screens below 640px"
    - "User can tap any nav link in the mobile menu to navigate to that section"
    - "Tapping a nav link closes the mobile menu automatically"
    - "Header wordmark letter-spacing is reduced on narrow screens so it does not overflow"
    - "Header padding is tighter on mobile so content is not cramped or cut off"
    - "Hamburger icon is hidden on screens 640px and wider where inline nav is visible"
  artifacts:
    - path: "src/pages/NeuPage.jsx"
      provides: "Hamburger menu state, toggle button with ARIA attributes, mobile nav panel with close-on-click"
      contains: "menuOpen"
    - path: "src/pages/NeuPage.css"
      provides: "Hamburger button styles, mobile nav slide-in panel, X-animation on active state, responsive header spacing"
      contains: "neu-header__hamburger"
  key_links:
    - from: "src/pages/NeuPage.jsx"
      to: "src/pages/NeuPage.css"
      via: "className toggling with menuOpen state"
      pattern: "neu-header__hamburger--active|neu-header__nav--open"
---

<objective>
Add a mobile hamburger menu to the NeuPage header and optimize header spacing/typography for mobile viewports.

Purpose: On screens below 640px, all nav links are currently hidden with `display: none` and no mobile alternative exists. Users cannot navigate to any section. This plan adds a standard hamburger menu with slide-out nav panel and tightens header spacing for narrow screens.

Output: Updated NeuPage.jsx with hamburger toggle state and button, updated NeuPage.css with hamburger icon styles (including X-animation), slide-out mobile nav panel, and responsive header padding/letter-spacing.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-neupage-navigation-hero/01-RESEARCH.md
@src/pages/NeuPage.jsx
@src/pages/NeuPage.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add hamburger menu JSX and state to NeuPage.jsx</name>
  <files>src/pages/NeuPage.jsx</files>
  <action>
In the NeuPage component function, add a `menuOpen` state using useState (initialize to false).

Inside the `<header>` element, between the wordmark div and the `<nav>`, add a hamburger button:

```jsx
<button
  className={`neu-header__hamburger${menuOpen ? ' neu-header__hamburger--active' : ''}`}
  onClick={() => setMenuOpen(!menuOpen)}
  aria-label="Toggle navigation menu"
  aria-expanded={menuOpen}
>
  <span className="neu-header__hamburger-line" />
  <span className="neu-header__hamburger-line" />
  <span className="neu-header__hamburger-line" />
</button>
```

Add the `menuOpen` class toggle to the existing `<nav>` element:

```jsx
<nav className={`neu-header__nav${menuOpen ? ' neu-header__nav--open' : ''}`}>
```

For each `<a>` and `<button>` inside the nav, add `onClick={() => setMenuOpen(false)}` so the menu closes when a link is tapped. For the existing "Reach Out" button that already has an onClick, wrap both calls:

```jsx
<button onClick={() => { setContactOpen(true); setMenuOpen(false); }} ...>
```

Do NOT change the "Access Platform" CTA link behavior — it already has its own visibility logic and should remain as-is.

Add `menuOpen` to the existing `useState` import if not already destructured.
  </action>
  <verify>
Run `npm run build` — no compilation errors. Visually inspect NeuPage.jsx to confirm:
1. `menuOpen` state exists
2. Hamburger button renders between wordmark and nav
3. Nav element has conditional `--open` class
4. All nav links call `setMenuOpen(false)` on click
  </verify>
  <done>NeuPage.jsx contains hamburger button with ARIA attributes, nav has open/close class toggle, and all links close the menu on tap.</done>
</task>

<task type="auto">
  <name>Task 2: Add hamburger and mobile nav CSS styles to NeuPage.css</name>
  <files>src/pages/NeuPage.css</files>
  <action>
Add the following CSS rules to NeuPage.css. Place the hamburger base styles AFTER the existing `.neu-header__cta:hover` rule block (around line 136) and BEFORE the laser section comment.

**Hamburger button base styles (hidden by default, shown on mobile):**

```css
.neu-header__hamburger {
  display: none;
  flex-direction: column;
  justify-content: space-around;
  width: 28px;
  height: 24px;
  background: transparent;
  border: none;
  cursor: pointer;
  padding: 0;
  z-index: 1001;
}

.neu-header__hamburger-line {
  width: 28px;
  height: 2px;
  background-color: var(--accent);
  border-radius: 1px;
  transition: all 0.3s ease;
  transform-origin: center;
}

.neu-header__hamburger--active .neu-header__hamburger-line:nth-child(1) {
  transform: translateY(9px) rotate(45deg);
}

.neu-header__hamburger--active .neu-header__hamburger-line:nth-child(2) {
  opacity: 0;
}

.neu-header__hamburger--active .neu-header__hamburger-line:nth-child(3) {
  transform: translateY(-9px) rotate(-45deg);
}
```

**Inside the existing `@media (max-width: 640px)` block, ADD these rules** (do not remove the existing rules already there):

```css
.neu-header {
  padding: 12px 16px;
}

.neu-header__wordmark {
  font-size: 11px;
  letter-spacing: 3px;
}

.neu-header__hamburger {
  display: flex;
}

.neu-header__nav {
  position: fixed;
  top: 0;
  right: 0;
  height: 100vh;
  height: 100dvh;
  width: 280px;
  max-width: 80vw;
  background: rgba(0, 0, 0, 0.98);
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
  border-left: 1px solid var(--border);
  flex-direction: column;
  align-items: flex-start;
  padding: 80px 32px 32px;
  gap: 32px;
  transform: translateX(100%);
  transition: transform 0.4s cubic-bezier(0.16, 1, 0.3, 1);
  z-index: 1000;
}

.neu-header__nav--open {
  transform: translateX(0);
}

.neu-header__link {
  display: block;
  width: 100%;
  font-size: 13px;
  padding: 8px 0;
  border-bottom: 1px solid var(--border);
}
```

**Important:** The existing `@media (max-width: 640px)` block already has `.neu-header__link { display: none; }`. This MUST be removed/replaced because we now want links visible inside the slide-out panel. Replace `display: none` with `display: block` as shown above. Also remove the existing `.neu-header__wordmark-sub { display: none; }` rule — keep wordmark-sub hidden on mobile but move it into the new nav rules if needed, or keep it as `display: none`.

Keep the existing `.neu-header__nav { gap: 12px; }` rule — it will be overridden by the new positional rules above.

The wordmark-sub should remain hidden on mobile (keep `display: none` for it).
  </action>
  <verify>
Run `npm run build` — no CSS errors. Open the dev server (`npm run dev`) and resize browser to 360px width. Confirm:
1. Hamburger icon (3 lime-green lines) appears in header
2. Tapping hamburger slides in a dark panel from the right
3. Nav links are visible inside the panel
4. Tapping hamburger again shows X-animation and closes panel
5. Header padding is tighter on mobile
6. Wordmark letter-spacing is reduced (3px instead of 6px)
7. On desktop (>640px), hamburger is hidden and inline nav is visible as before
  </verify>
  <done>Hamburger icon appears on mobile with X-animation toggle, slide-out nav panel works with smooth transition, header spacing and wordmark typography are optimized for narrow viewports, and desktop layout is unchanged.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes without errors
2. At 360px viewport width: hamburger icon visible, tapping opens slide-out nav, links are clickable and close the menu
3. At 1024px viewport width: no hamburger visible, inline nav links displayed as before
4. Header wordmark reads clearly at 360px with reduced letter-spacing
5. All ARIA attributes present on hamburger button (aria-label, aria-expanded)
</verification>

<success_criteria>
- NAV-01 satisfied: Mobile hamburger menu with slide-out nav panel exists and functions
- NAV-02 satisfied: Header padding reduced to 12px 16px on mobile
- NAV-03 satisfied: Wordmark letter-spacing reduced to 3px and font-size to 11px below 640px
- No regressions on desktop layout
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/01-neupage-navigation-hero/01-01-SUMMARY.md`
</output>
